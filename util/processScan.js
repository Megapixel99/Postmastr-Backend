
module.exports = function (rawData) {
    var recipient = '';
    var sender = '';
    var carrierName = '';
    var returnAddress = '';
    var trackingNumber;
    var dateReceived = new Date.toDateString();
    //store individual lines from 
    textArray = rawData.split("\n");
    textArray.forEach(
        //process function 
        console.log(textArray[index])
    );







} 